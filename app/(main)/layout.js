const MainLayout = ({children}) => { 
    //this is the main layout for the main app used to redirect user after Registration
  return <div className="container mx-auto mt-24 mb-20">{children}</div>;
};
export default MainLayout;